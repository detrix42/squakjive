
<div class="card user-info-card">
  <div class="row g-0">
    <div class="col-2">
      <%# If you're using Active Storage for avatar, you can use something like: %>
      <%# <%= image_tag current_user.avatar if current_user.avatar.attached? %>
      <%# For now, using a placeholder %>
      <%= image_tag robohash_avatar_url(@username, size: "60x60"), class: "rounded-circle m-2", alt: @username %>

    </div>
    <div class="col">
      <div class="card-body">
        <h6 class="card-title fw-bold mb-0"><%= @username %></h6>
        <p class="card-text text-muted"><small>@<%= @email %></small></p>
      </div>
    </div>
    <div class="col-1" id="user-action-col">
      <div class="logout">
        <%= button_to destroy_user_session_path,
                      method: :delete,
                      class: "text-danger bg-transparent border-0",
                      form: { data: { turbo: true } } do %>
          <i class="bi bi-box-arrow-right ms-1"></i>
        <% end %>

      </div>

    </div>
  </div>
</div>
