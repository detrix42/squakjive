test:
  service: Disk
  root: <%= Rails.root.join("tmp/storage") %>

local:
  service: Disk
  root: <%= Rails.root.join("storage") %>

# Local disk storage. In production (Kamal on roci) we use a bind mount (see deploy.yml)
# so you can point uploads to any host directory you want (e.g. a dedicated disk/partition
# for large video files).
roci:
  service: Disk
#  root: <%= ENV.fetch("ACTIVE_STORAGE_ROOT", "/rails/storage") %>
  root: <%= ENV.fetch("ACTIVE_STORAGE_ROOT", Rails.root.join("storage")) %>

# In your setup (Docker + systemd on roci), map the host directory with a volume/bind mount:
#   -v /home/SquakJive_storage:/rails/storage
# Then set in /etc/squakjive.env :
#   ACTIVE_STORAGE_ROOT=/rails/storage
# All uploads (including videos) will then physically live under /home/SquakJive_storage on the host.

# Use bin/rails credentials:edit to set the AWS secrets (as aws:access_key_id|secret_access_key)
# amazon:
#   service: S3
#   access_key_id: <%= Rails.application.credentials.dig(:aws, :access_key_id) %>
#   secret_access_key: <%= Rails.application.credentials.dig(:aws, :secret_access_key) %>
#   region: us-east-1
#   bucket: your_own_bucket-<%= Rails.env %>

# Remember not to checkin your GCS keyfile to a repository
# google:
#   service: GCS
#   project: your_project
#   credentials: <%= Rails.root.join("path/to/gcs.keyfile") %>
#   bucket: your_own_bucket-<%= Rails.env %>

# Use bin/rails credentials:edit to set the Azure Storage secret (as azure_storage:storage_access_key)
# microsoft:
#   service: AzureStorage
#   storage_account_name: your_account_name
#   storage_access_key: <%= Rails.application.credentials.dig(:azure_storage, :storage_access_key) %>
#   container: your_container_name-<%= Rails.env %>

# mirror:
#   service: Mirror
#   primary: local
#   mirrors: [ amazon, google, microsoft ]
